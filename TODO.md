bug with eval:
* no caching
* poor progress bar (should i open an issue to deepeval?)
* current approach: gather all run results and run eval. is there a better way? maybe I should pick a run i want to eval?
* also, maybe it makes sense to add a script that prints a table of result for a run / several runs?